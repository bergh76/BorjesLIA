@using Microsoft.AspNet.Identity

    @if (Request.IsAuthenticated)
    {
        using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
        {
            @Html.AntiForgeryToken()
            <ul class="nav navbar-nav">
                <li>
                    @Html.ActionLink("Inloggad som: " + User.Identity.GetUserName(), "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
                </li>
                <li>@Html.ActionLink("Inställningar", "Index", "Hub")</li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
            </ul>
        }
    }
    else
    {
        @*<ul class="nav navbar-nav navbar-right">
            <li>
                <input type="button" class="btn btn-primary btn-sm" data-toggle="modal" data-target="#myModal">
                    @Html.ActionLink("Inlogging", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })
            </li>
        </ul>*@

        <ul class="nav navbar-nav navbar-right">

            <li><span class="nav navbar-brand"><i class="fa fa-lock"></i> @Html.ActionLink("Logga in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</span></li>

        </ul>
    }

